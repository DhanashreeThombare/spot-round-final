# Python Dependencies (for extract_tables.py)
tabula-py==2.10.0
pandas==2.0.0
numpy==2.0.0
PyPDF2==3.0.1
jpype1==1.5.0  # Required for tabula-py's Java integration
python-dateutil==2.9.0.post0  # pandas dependency


# Node.js Dependencies (listed in your server.js)
# Note: Node.js packages are managed in package.json, but included here for reference
# flask==3.0.0  # (Uncomment if you need Flask for any Python endpoints)
